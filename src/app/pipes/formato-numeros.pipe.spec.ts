import { FormatoNumerosPipe } from './formato-numeros.pipe';

describe('FormatoNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
